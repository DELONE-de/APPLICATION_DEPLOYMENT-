AWS_REGION=us-east-1
DYNAMODB_TABLE=dev-items
# For local development with DynamoDB Local (only uncomment if running dynamodb-local):
# DYNAMODB_ENDPOINT=http://localhost:8000
NEXT_PUBLIC_BASE_URL=http://localhost:3000
